services:
  airflow:
    build:
      context: ./dockerfiles/airflow
      dockerfile: ./dockerfile
    ports:
      - 8888:8888
      - 8080:8080
    volumes:
      - ./:/knowledge
      - /tmp/knowledge_dags:/root/airflow/dags # to edit dags locally

