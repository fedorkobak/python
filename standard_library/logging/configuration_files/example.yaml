version: 1
formatters:
    simpleFormatter :
        format : "<-- %(message)s -->"
handlers:
    consoleHandler:
        class: "logging.StreamHandler"
        formatter: "simpleFormatter"
loggers:
    simpleExample:
        level: DEBUG
        handlers:
            - "consoleHandler"
