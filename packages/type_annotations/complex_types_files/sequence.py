from typing import Sequence
# Annotatate type once for all elements
# list of strings 
my_sequence: Sequence[str|int] = ["a", "b", "c"]
# tuple of ints
my_sequence = (1, 4, 2, 8)

# set is not sequence
my_sequence = {1,"2"}
